import contractsConfig from "@/contracts-config.json";

// Centralized contracts exposure for Web3 features
// Extend this as more contracts are added
export const POINTS_CONTRACT_ADDRESS = (contractsConfig as any)
  ?.contractAddress as `0x${string}`;

